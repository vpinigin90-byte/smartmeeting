meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: []
  artifacts:
    "*": /

run:
  command: npm start
  persistenceMount: /data
  containerPort: 3000
  servicePort: 80
